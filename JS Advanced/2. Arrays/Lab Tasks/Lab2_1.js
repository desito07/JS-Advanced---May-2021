function solve(arr) {
    let fisrtArg = Number(arr[0]);
    let lastArg = Number(arr[arr.length - 1]);
    let sum = fisrtArg + lastArg;
    console.log(sum);
  }
  solve(["20", "30", "40"]);
  